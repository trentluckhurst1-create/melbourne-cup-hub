let matrixReady=false;
let matrixFilter='all';
let matrixSearch='';
let matrixSort='projected';

function latestLeadup(name){return (leadupData?.events||[]).filter(x=>x.horse===name).sort((a,b)=>b.date.localeCompare(a.date))[0]||null;}
function publicFormRec(name){return formIntelData?.horses?.[name]||null;}
function pfrEvidence(name){
  const p=typeof pfrProfile==='function'?pfrProfile(name):null;
  if(p?.source==='PRIMARY')return {state:'Private',value:p.currentDisplay||p.current,kind:'private'};
  if(p?.source==='PUBLIC_PFR')return {state:'Public',value:p.currentDisplay||p.current,kind:'public'};
  if(p?.source==='RAS_FALLBACK')return {state:'R&S',value:p.currentDisplay||p.current,kind:'ras'};
  return {state:'Gap',value:null,kind:'gap'};
}
function staminaBand(name){
  const runs=typeof publicActualRuns==='function'?publicActualRuns(name):(publicFormRec(name)?.runs||[]);
  if(!runs.length)return 'Researching';
  const distances=runs.map(r=>Number(r.distanceM)||0);const max=Math.max(...distances);
  const wins=runs.filter(r=>/^1(st)?$/i.test(String(r.finish||''))||/^1st$/i.test(String(r.finish||'')));
  const winMax=wins.length?Math.max(...wins.map(r=>Number(r.distanceM)||0)):0;
  if(winMax>=3200)return '3200m winner';
  if(runs.some(r=>Number(r.distanceM)>=3200&&/^(2|3)(nd|rd)?$/i.test(String(r.finish||''))))return '3200m placed';
  if(max>=3200)return '3200m exposed';if(winMax>=3000)return '3000m+ winner';if(max>=3000)return '3000m+ exposed';if(winMax>=2800)return '2800m+ winner';if(max>=2800)return '2800m+ exposed';if(max>=2400)return '2400m+ evidence';return '<2400m loaded';
}
function qualificationState(name){const q=(qualificationData?.qualified||[]).find(x=>x.horse===name);if(q)return 'Golden Ticket';const l=latestLeadup(name);if(l?.status==='Golden Ticket')return 'Golden Ticket';return 'No exemption';}
function currentCampaign(name){const l=latestLeadup(name);if(!l)return 'Researching';if(l.status==='Golden Ticket')return `${l.result||'Qualified'} · ${l.race}`;if(l.result&&l.result!=='Pending')return `${l.result} · ${l.race}`;return `${l.status||l.type} · ${l.race}`;}
function readinessScore(name){let n=0;if((typeof publicActualRuns==='function'?publicActualRuns(name):(publicFormRec(name)?.runs||[])).length)n++;if(staminaBand(name)!=='Researching')n++;if(weightRec(name))n++;if(pfrEvidence(name).kind!=='gap')n++;if(baseRecord(name))n++;if(qualificationState(name)==='Golden Ticket'||projectedRec(name)||latestLeadup(name))n++;return n;}
function stayingEvidence(name){return /3200m winner|3200m placed|3000m\+ winner|2800m\+ winner/i.test(staminaBand(name));}
function researchGap(name){return readinessScore(name)<6||(typeof publicActualRuns==='function'?publicActualRuns(name):(publicFormRec(name)?.runs||[])).length<5||!baseRecord(name)||pfrEvidence(name).kind==='gap'||!latestLeadup(name);}
function matrixFilterMatch(h){const name=h.horse,q=matrixSearch.trim().toLowerCase();if(q&&!`${name} ${h.trainer} ${h.country}`.toLowerCase().includes(q))return false;if(matrixFilter==='projected'&&!projectedRec(name))return false;if(matrixFilter==='golden'&&qualificationState(name)!=='Golden Ticket')return false;if(matrixFilter==='international'&&h.trainingRegion!=='International')return false;if(matrixFilter==='stayers'&&!stayingEvidence(name))return false;if(matrixFilter==='gaps'&&!researchGap(name))return false;return true;}
function matrixSortedRows(){const rows=(cupData?.horses||[]).filter(matrixFilterMatch);return rows.sort((a,b)=>{if(matrixSort==='weight')return (weightRec(b.horse)?.predictedKg??-999)-(weightRec(a.horse)?.predictedKg??-999)||a.nominationNumber-b.nominationNumber;if(matrixSort==='readiness')return readinessScore(b.horse)-readinessScore(a.horse)||a.nominationNumber-b.nominationNumber;if(matrixSort==='nomination')return a.nominationNumber-b.nominationNumber;const ar=projectedRec(a.horse)?.rank??999,br=projectedRec(b.horse)?.rank??999;return ar-br||readinessScore(b.horse)-readinessScore(a.horse)||a.nominationNumber-b.nominationNumber;});}
function matrixFilterButton(id,label,count=''){return `<button class="ghost-button matrix-filter ${matrixFilter===id?'active':''}" data-matrix-filter="${id}">${label}${count!==''?` <span>${count}</span>`:''}</button>`;}
function intelligenceMatrixView(){
 const horses=cupData?.horses||[],full=horses.filter(h=>readinessScore(h.horse)===6).length,privatePfr=horses.filter(h=>pfrEvidence(h.horse).kind==='private').length,anyPfr=horses.filter(h=>pfrEvidence(h.horse).kind!=='gap').length,bases=horses.filter(h=>!!baseRecord(h.horse)).length,weights=horses.filter(h=>!!weightRec(h.horse)).length,projected=horses.filter(h=>!!projectedRec(h.horse)).length,golden=horses.filter(h=>qualificationState(h.horse)==='Golden Ticket').length,international=horses.filter(h=>h.trainingRegion==='International').length,stayers=horses.filter(h=>stayingEvidence(h.horse)).length,gaps=horses.filter(h=>researchGap(h.horse)).length,visible=matrixSortedRows();
 if(typeof assertCupUniverse==='function'&&matrixFilter==='all'&&!matrixSearch)assertCupUniverse(visible,'Cup Intelligence Workbench');
 return `<div class="section-header"><div><div class="kicker">101-HORSE DECISION BOARD</div><h2>Cup Intelligence Workbench</h2><div class="section-copy">One row per official nominee. Missing evidence is exposed, never used to suppress a horse.</div></div></div><section class="metric-grid">${metric('Official Nominees',horses.length,'Full nomination universe')}${metric('Any PFR Evidence',`${anyPfr}/${horses.length}`,'Private/public/R&S fallback')}${metric('Private PFR',`${privatePfr}/${horses.length}`,'Captured private ratings')}${metric('Weights',`${weights}/${horses.length}`,'Predicted pre-release')}${metric('Current Bases',`${bases}/${horses.length}`,'Horse-specific verified')}${metric('6/6 Evidence',`${full}/${horses.length}`,'Complete workbench lanes')}</section><div class="panel"><div class="panel-head"><div><h3>All-Nominee Intelligence Matrix</h3><div class="panel-sub">Readiness is evidence coverage only — never a rating or betting score.</div></div><span class="tag gold">${visible.length}/${horses.length} VISIBLE</span></div><div class="matrix-toolbar" style="display:flex;gap:8px;flex-wrap:wrap;align-items:center;padding:12px 14px;border-bottom:1px solid var(--line,#202a36)"><input id="matrix-search" value="${matrixSearch.replace(/"/g,'&quot;')}" placeholder="Search horse, trainer, country…" style="min-width:250px;flex:1;background:#0a111b;border:1px solid #263241;color:inherit;padding:9px 10px;border-radius:4px"/><select id="matrix-sort" style="background:#0a111b;border:1px solid #263241;color:inherit;padding:9px 10px;border-radius:4px"><option value="projected" ${matrixSort==='projected'?'selected':''}>Sort: Projected rank</option><option value="weight" ${matrixSort==='weight'?'selected':''}>Sort: Predicted weight</option><option value="readiness" ${matrixSort==='readiness'?'selected':''}>Sort: Research readiness</option><option value="nomination" ${matrixSort==='nomination'?'selected':''}>Sort: Nomination #</option></select></div><div class="matrix-toolbar" style="display:flex;gap:8px;flex-wrap:wrap;padding:10px 14px;border-bottom:1px solid var(--line,#202a36)">${matrixFilterButton('all','All',horses.length)}${matrixFilterButton('projected','Projected',projected)}${matrixFilterButton('golden','Golden Ticket',golden)}${matrixFilterButton('international','International',international)}${matrixFilterButton('stayers','Staying Evidence',stayers)}${matrixFilterButton('gaps','Research Gaps',gaps)}</div><div class="table-wrap"><table class="data-table"><thead><tr><th>#</th><th>Horse</th><th>Form Runs</th><th>Stamina</th><th>Pred. Wt</th><th>PFR Evidence</th><th>Current Base</th><th>Qualification</th><th>Projected</th><th>Latest Campaign</th><th>Ready</th></tr></thead><tbody>${visible.map(h=>{const name=h.horse,f=(typeof publicActualRuns==='function'?publicActualRuns(name):(publicFormRec(name)?.runs||[])).length,w=weightRec(name),p=projectedRec(name),pf=pfrEvidence(name),score=readinessScore(name);const pfrCell=pf.kind==='private'?tag(`PRIVATE ${pf.value}`,'green'):pf.kind==='public'?tag(`PUBLIC ${pf.value}`,'gold'):pf.kind==='ras'?tag(`R&S ${pf.value}`,'gold'):tag('GAP','red');return `<tr><td>${h.nominationNumber}</td><td class="horse">${horseLink(name)}</td><td>${f||'—'}</td><td>${staminaBand(name)}</td><td>${w?`${w.predictedKg.toFixed(1)}kg`:'—'}</td><td>${pfrCell}</td><td>${baseCell(name)}</td><td>${qualificationState(name)==='Golden Ticket'?tag('Golden Ticket','green'):'<span class="muted">No exemption</span>'}</td><td>${p?`#${p.rank}`:'—'}</td><td class="wrap-cell">${currentCampaign(name)}</td><td>${score}/6</td></tr>`}).join('')||'<tr><td colspan="11" class="muted">No horses match the current filters.</td></tr>'}</tbody></table></div></div>`;
}
function bindMatrixControls(){const search=document.getElementById('matrix-search');if(search){search.oninput=e=>{matrixSearch=e.target.value;const pos=e.target.selectionStart;document.getElementById('app-content').innerHTML=intelligenceMatrixView();bindMatrixControls();const next=document.getElementById('matrix-search');if(next){next.focus();next.setSelectionRange(pos,pos);}};}const sort=document.getElementById('matrix-sort');if(sort)sort.onchange=e=>{matrixSort=e.target.value;document.getElementById('app-content').innerHTML=intelligenceMatrixView();bindMatrixControls();};document.querySelectorAll('[data-matrix-filter]').forEach(btn=>btn.onclick=()=>{matrixFilter=btn.dataset.matrixFilter;document.getElementById('app-content').innerHTML=intelligenceMatrixView();bindMatrixControls();});}
const matrixRenderBase=render;render=function(view='dashboard'){if(view==='analysis'){document.getElementById('page-title').textContent='Race Analysis';const root=document.getElementById('app-content');if(!cupData||!formIntelData||!weightPredictions||!projectedFieldData||!qualificationData){root.innerHTML='<div class="placeholder">Loading full-field intelligence matrix…</div>';Promise.all([typeof loadFormIntel==='function'?loadFormIntel():Promise.resolve(),typeof loadExtras==='function'?loadExtras():Promise.resolve(),typeof loadProjectedField==='function'?loadProjectedField():Promise.resolve(),typeof loadLeadups==='function'?loadLeadups():Promise.resolve()]).then(()=>render(view));return;}root.innerHTML=intelligenceMatrixView();setTimeout(bindMatrixControls,0);return;}matrixRenderBase(view);};matrixReady=true;
