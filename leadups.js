let leadupData=null;
let leadupPromise=null;
let leadupFilter='all';
let leadupQuery='';
function loadLeadups(){if(leadupPromise)return leadupPromise;leadupPromise=fetch('./data/leadups/2026-09-13.json',{cache:'no-store'}).then(r=>r.ok?r.json():null).then(d=>{leadupData=d;return d;}).catch(()=>null);return leadupPromise;}
function leadupStatusTag(status){if(status==='Golden Ticket')return tag(status,'green');if(status==='Completed')return tag(status,'gold');if(status==='Declared'||status==='Due to race')return tag(status,'green');if(status==='Did not start')return tag(status,'red');return `<span class="tag">${status}</span>`;}
function leadupProjected(name){return !!(projectedFieldData?.projected24||[]).find(x=>x.horse===name);}
function leadupInternational(name){return horseByName(name)?.trainingRegion==='International';}
function leadupFilteredEvents(){const q=leadupQuery.trim().toLowerCase();return [...(leadupData?.events||[])].sort((a,b)=>b.date.localeCompare(a.date)||a.horse.localeCompare(b.horse)).filter(x=>{if(q&&!`${x.horse} ${x.race||''} ${x.track||''} ${x.note||''}`.toLowerCase().includes(q))return false;if(leadupFilter==='results'&&x.type!=='Result')return false;if(leadupFilter==='tickets'&&x.status!=='Golden Ticket')return false;if(leadupFilter==='pending'&&!['Declared','Due to race'].includes(x.status))return false;if(leadupFilter==='projected'&&!leadupProjected(x.horse))return false;if(leadupFilter==='international'&&!leadupInternational(x.horse))return false;return true;});}
function setLeadupFilter(v){leadupFilter=v;render('leadups');}function setLeadupQuery(v){leadupQuery=v;render('leadups');setTimeout(()=>{const e=document.getElementById('leadup-search');if(e){e.focus();e.setSelectionRange(e.value.length,e.value.length);}},0);}window.setLeadupFilter=setLeadupFilter;window.setLeadupQuery=setLeadupQuery;
function leadupControls(){const filters=[['all','All events'],['projected','Projected 24'],['results','Results'],['tickets','Golden Tickets'],['pending','Pending'],['international','International']];return `<div class="order-tools"><input id="leadup-search" class="order-search" value="${leadupQuery.replace(/"/g,'&quot;')}" placeholder="Search horse, race, track or note…" oninput="setLeadupQuery(this.value)">${filters.map(([k,l])=>`<button class="order-filter ${leadupFilter===k?'active':''}" onclick="setLeadupFilter('${k}')">${l}</button>`).join('')}</div>`;}
function nomineePrepCoverage(){const names=(cupData?.horses||[]).map(x=>x.horse),events=leadupData?.events||[],withEvent=names.filter(n=>events.some(e=>e.horse===n)),missing=names.filter(n=>!events.some(e=>e.horse===n));return {names,withEvent,missing};}
function leadupsView(){const d=leadupData;if(!d)return '<div class="placeholder">Loading lead-up tracker…</div>';const all=[...(d.events||[])].sort((a,b)=>b.date.localeCompare(a.date)||a.horse.localeCompare(b.horse)),events=leadupFilteredEvents(),starters=all.filter(x=>x.type==='Result').length,tickets=all.filter(x=>x.status==='Golden Ticket').length,pending=all.filter(x=>['Declared','Due to race'].includes(x.status)).length,latest=all[0]||null,pc=nomineePrepCoverage();return `<div class="section-header"><div><div class="kicker">101-Horse Campaign Timeline · Snapshot ${d.snapshotDate}</div><h2>Lead-up Preparation Control</h2><div class="section-copy">Every official nominee remains in coverage. A missing event is a visible research gap, never a reason to remove the horse.</div></div></div><section class="metric-grid">${metric('Universe',pc.names.length,'Official nominees')}${metric('Prep Tracked',`${pc.withEvent.length}/${pc.names.length}`,'Nominees with dated campaign evidence')}${metric('Coverage Gaps',pc.missing.length,'Nominees requiring campaign research')}${metric('Results',starters,'Completed lead-up runs')}${metric('Pending Starts',pending,'Declared / sourced engagements')}</section><div class="panel"><div class="panel-head"><div><h3>Campaign Event Ledger</h3><div class="panel-sub">Dated evidence currently loaded across the full nomination universe.</div></div><span class="tag gold">${events.length} EVENTS</span></div>${leadupControls()}<div class="table-wrap"><table class="data-table"><thead><tr><th>Date</th><th>Horse</th><th>Projected</th><th>Race / Event</th><th>Track</th><th>Distance</th><th>Result</th><th>Status</th><th>Campaign Note</th></tr></thead><tbody>${events.length?events.map(x=>`<tr><td>${new Date(x.date+'T00:00:00').toLocaleDateString('en-AU',{day:'2-digit',month:'short'})}</td><td class="horse">${horseByName(x.horse)?horseLink(x.horse):x.horse}</td><td>${leadupProjected(x.horse)?tag('Top 24','green'):'—'}</td><td>${x.race}</td><td>${x.track||'—'}</td><td>${x.distanceM?`${x.distanceM}m`:'—'}</td><td><strong>${x.result||'—'}</strong></td><td>${leadupStatusTag(x.status)}</td><td class="wrap-cell">${x.note||'—'}</td></tr>`).join(''):'<tr><td colspan="9" class="order-empty">No campaign events match this filter.</td></tr>'}</tbody></table></div></div><div class="panel spaced-panel"><div class="panel-head"><div><h3>All-Nominee Preparation Coverage</h3><div class="panel-sub">All 101 nominations are audited. Horses without a stored campaign event remain visible below.</div></div><span class="tag ${pc.missing.length?'gold':'green'}">${pc.withEvent.length}/${pc.names.length}</span></div><div class="table-wrap"><table class="data-table"><thead><tr><th>#</th><th>Horse</th><th>Region</th><th>Projected</th><th>Latest Tracked Event</th><th>Coverage</th></tr></thead><tbody>${(cupData?.horses||[]).map(h=>{const ev=all.find(e=>e.horse===h.horse);return `<tr><td>${h.nominationNumber}</td><td class="horse">${horseLink(h.horse)}</td><td>${h.trainingRegion||'—'}</td><td>${leadupProjected(h.horse)?`#${projectedRec(h.horse)?.rank}`:'—'}</td><td>${ev?`${ev.date} · ${ev.race}`:'—'}</td><td>${ev?tag('Tracked','green'):tag('Research gap','gold')}</td></tr>`}).join('')}</tbody></table></div></div>`;}
const leadupRenderBase=render;render=function(view='dashboard'){if(view==='leadups'){document.getElementById('page-title').textContent='Lead-up Tracker';const root=document.getElementById('app-content');if(!leadupData){root.innerHTML='<div class="placeholder">Loading lead-up tracker…</div>';Promise.all([loadLeadups(),typeof loadProjectedField==='function'?loadProjectedField():Promise.resolve()]).then(()=>render('leadups'));return;}root.innerHTML=leadupsView();return;}leadupRenderBase(view);};loadLeadups();
