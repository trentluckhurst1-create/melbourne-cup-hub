let internationalReady=false;
let internationalPrepData=null;
let internationalPrepPromise=null;
function loadInternationalPrep(){if(internationalPrepPromise)return internationalPrepPromise;internationalPrepPromise=fetch('./data/intelligence/2026-09-13-international-preparation.json',{cache:'no-store'}).then(r=>r.ok?r.json():null).then(d=>{internationalPrepData=d;return d;}).catch(()=>null);return internationalPrepPromise;}
function prepRecord(name){return internationalPrepData?.horses?.[name]||null;}
function latestLeadupFor(name){const events=(leadupData?.events||[]).filter(e=>e.horse===name).sort((a,b)=>String(b.date).localeCompare(String(a.date)));return events[0]||null;}
function qualificationFor(name){const q=(qualificationData?.qualified||qualificationData?.horses||[]);if(Array.isArray(q))return q.find(x=>x.horse===name)||null;return null;}
function internationalRaidersView(){const universe=cupData?.horses||[],horses=universe.filter(h=>h.trainingRegion==='International'),domestic=universe.length-horses.length,withBase=horses.filter(h=>baseRecord(h.horse)).length,withWeight=horses.filter(h=>weightRec(h.horse)).length,projected=horses.filter(h=>projectedRec(h.horse)).length,prepVerified=horses.filter(h=>prepRecord(h.horse)).length,ireland=horses.filter(h=>['Aidan O’Brien','Joseph O’Brien','Emmet Mullins'].includes(h.trainer)).length;return `<div class="section-header"><div><div class="kicker">Specialist Lens · ${horses.length} of ${universe.length} Nominees</div><h2>International Raiders</h2><div class="section-copy">The Hub universe remains all ${universe.length} official nominations. This specialist workspace intentionally focuses on the ${horses.length} overseas-trained nominees; the other ${domestic} remain in the master universe and are marked not applicable to this international lens rather than treated as missing coverage.</div></div></div><section class="metric-grid">${metric('Cup Universe',universe.length,'All official nominees')}${metric('International',horses.length,'In-scope for this specialist lens')}${metric('Domestic / N/A',domestic,'Still retained in master universe')}${metric('Ireland',ireland,'Aidan / Joseph O’Brien + Emmet Mullins')}${metric('Current Base Verified',withBase,`${withBase}/${horses.length} international nominees`)}${metric('Prep Status',prepVerified,`${prepVerified}/${horses.length} verified campaign records`)}${metric('Projected 24',projected,'Signal only; never membership')}</section><div class="panel"><div class="panel-head"><div><h3>International Contender Board</h3><div class="panel-sub">Specialist subset of the 101-horse universe. Current location remains “Researching” unless verified for the individual horse. Campaign status does not imply confirmed travel unless the source says so.</div></div><span class="tag gold">${horses.length}/${universe.length} SPECIALIST LENS</span></div><div class="table-wrap"><table class="data-table"><thead><tr><th>#</th><th>Horse</th><th>Trainer</th><th>Trainer Base</th><th>Current Horse Base</th><th>Prep Status</th><th>Weight</th><th>Projected</th><th>Qualification</th><th>Latest Lead-up</th></tr></thead><tbody>${horses.map(h=>{const tr=trainerRecord(h.trainer),w=weightRec(h.horse),p=projectedRec(h.horse),q=qualificationFor(h.horse),l=latestLeadupFor(h.horse),prep=prepRecord(h.horse);return `<tr><td>${h.nominationNumber}</td><td class="horse">${horseLink(h.horse)}</td><td>${h.trainer}</td><td>${tr?.primaryBase||'Researching'}</td><td>${baseCell(h.horse)}</td><td class="wrap-cell">${prep?`${prep.campaignStatus}<br><span class="muted">${prep.nextRun}</span>`:'<span class="muted">Researching</span>'}</td><td>${w?`${Number(w.predictedKg).toFixed(1)}kg`:'—'}</td><td>${p?`#${p.rank} · ${p.band||''}`:'—'}</td><td>${q?tag(q.status||'Qualified','green'):'<span class="muted">Standard ballot</span>'}</td><td>${l?`${l.date} · ${l.race} · ${l.result||l.status}`:'<span class="muted">Researching</span>'}</td></tr>`}).join('')}</tbody></table></div></div><section class="profile-grid"><div class="panel"><h3>International Monitoring</h3><div class="rule-list"><div class="rule-row"><span>01</span><p>Track European and Japanese preparation races without assuming participation.</p></div><div class="rule-row"><span>02</span><p>Separate trainer headquarters from each horse's actual current preparation location.</p></div><div class="rule-row"><span>03</span><p>Record quarantine, travel and CT/veterinary status only when confirmed.</p></div><div class="rule-row"><span>04</span><p>Distinguish a confirmed traveller from a horse for whom Melbourne remains only an option.</p></div><div class="rule-row"><span>05</span><p>Keep private PFR evidence as a separate authorised layer; never substitute it for missing public evidence.</p></div></div></div><div class="panel"><h3>Travel & Vet Windows</h3><div class="profile-list"><div><span>Shipment 1 quarantine / CT</span><strong>3–10 Oct</strong></div><div><span>Shipment 2 quarantine / CT</span><strong>17–24 Oct</strong></div><div><span>Pre-Melbourne CT window</span><strong>16–29 Oct</strong></div><div><span>Vet inspection #1</span><strong>29–30 Oct</strong></div><div><span>Vet inspection #2</span><strong>2 Nov</strong></div></div></div></section>`;}
const internationalRenderBase=render;render=function(view='dashboard'){if(view==='international'){document.getElementById('page-title').textContent='International Raiders';const root=document.getElementById('app-content'),pending=[];if(!trainerBaseData)pending.push(loadTrainerBases());if(!leadupData)pending.push(loadLeadups());if(!qualificationData||!weightPredictions)pending.push(loadExtras());if(!projectedFieldData)pending.push(loadProjectedField());if(!internationalPrepData)pending.push(loadInternationalPrep());if(pending.length){root.innerHTML='<div class="placeholder">Loading international intelligence…</div>';Promise.all(pending).then(()=>render(view));return;}root.innerHTML=internationalRaidersView();return;}internationalRenderBase(view);};loadInternationalPrep();